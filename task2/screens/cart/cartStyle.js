import { Dimensions, StyleSheet } from "react-native";

const CartStyle = StyleSheet.create({
    style : {
        flex : 1,
        alignItems : "center",
        justifyContent : "center"
    },
    text : {
        fontSize : 50,
    
    }
})

module.exports = {CartStyle}